 
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class=container-fluid>
    <div class="navbar-header">
      
      <%= link_to "Alpha blog", root_path, class: "navbar-brand", id: "logo" %>
      
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
        
    </div>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <% if logged_in? %>
      <ul class="navbar-nav mr-auto"> 
        <li>
         
          <form id="search" class="form-inline my-2 my-lg-0" method="get">
            <input class="no_border" id="filter" type="text" placeholder="Search" name='search' />    
            <i id="filtersubmit" type='submit' class="fa fa-search"></i>
            <input id="tags_list" name="tags_list" type="hidden" />
          </form>  
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Articles options
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            
            <a <%= link_to 'New Article', new_article_path, class: 'dropdown-item' %></a>
            <a><%= link_to "Articles", articles_path, class: "dropdown-item" %></a>
            
          </div>   
        </li>
      </ul>
    
      <div>
        <ul class="navbar-nav mr-auto">
         <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <% current_user.admin? ? user_drop_name = 'Admin' : user_drop_name = current_user.username %>
              <%= user_drop_name %>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
        
              <% if current_user.admin? %>
                 <a <%= link_to "All users", users_path, class: "nav-link" %></a>
              <% end %>
              <a <%= link_to "My articles", user_path(User.find(session[:user_id])), class: "nav-link" %></a>
              <div class="dropdown-divider"></div>
              <a <%= link_to "My profile", edit_user_path(User.find(session[:user_id])), class: "nav-link" %></a>
              <div class="dropdown-divider"></div>
              <a class="dropdown-item" href="#">My preferences</a>
              <div class="dropdown-divider"></div>
              <a <%= link_to "Log out", logout_path, class: "nav-link", method: :delete %></a>
              
            </div>
          </li>
        </ul>
      </div>
    
    <% else %>

      <form class="form-inline my-2 my-lg-0">
        <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
      </form>
      <div class="nav-item">
        <a <%= link_to "Log in", login_path, class: "nav-link" %> </a>
      </div>
   
    <% end %>
    </div>
  </div>
</nav>